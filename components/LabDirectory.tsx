import Link from "next/link";import {labs} from "@/data/labs";
export function LabDirectory(){return <section className="container section" id="lab-directory"><div className="section-head"><span className="kicker">Lab directory</span><h2>Explore individual labs.</h2><p>Trang chủ liệt kê các lab như một directory. Click vào từng lab để vào trang giới thiệu riêng, thay vì dẫn tới một trang labs filter.</p></div><div className="lab-directory-grid">{labs.map(l=><Link className="lab-directory-card" href={`/labs/${l.id}`} key={l.id}><span className="code">{l.code}</span><h3>{l.name}</h3><p>{l.desc}</p><div className="meta"><span>F{l.floor} · {l.room}</span><span>open →</span></div></Link>)}</div></section>}
